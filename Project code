# Project code 

from google.colab import drive
drive.mount('/content/drive')

import os
import zipfile
dataset1_zip = "/content/drive/MyDrive/Indian_bovine_breed.zip"
dataset2_zip = "/content/drive/MyDrive/Indian cattle breed Image.zip"
merged_path = "/content/merged_dataset/"
os.makedirs(merged_path, exist_ok=True)

def unzip_dataset(zip_path, extract_to):
    with zipfile.ZipFile(zip_path, 'r') as zip_ref:
        zip_ref.extractall(extract_to)

unzip_dataset(dataset1_zip, merged_path)
unzip_dataset(dataset2_zip, merged_path)

print("Datasets unzipped and merged at:", merged_path)


import tensorflow as tf
from tensorflow.keras.preprocessing.image import ImageDataGenerator

IMG_SIZE = 224  
BATCH_SIZE = 32

datagen = ImageDataGenerator(
    rescale=1./255,
    validation_split=0.2,  # 80-20 split
    horizontal_flip=True,
    rotation_range=20,
    zoom_range=0.15,
    width_shift_range=0.2,
    height_shift_range=0.2
)

# Training Data
train_data = datagen.flow_from_directory(
    merged_path,
    target_size=(IMG_SIZE, IMG_SIZE),
    batch_size=BATCH_SIZE,
    subset='training',
    class_mode='categorical'
)

# Validation Data
val_data = datagen.flow_from_directory(
    merged_path,
    target_size=(IMG_SIZE, IMG_SIZE),
    batch_size=BATCH_SIZE,
    subset='validation',
    class_mode='categorical'
)

# Add custom classification
x = base_model.output
x = GlobalAveragePooling2D()(x)
x = Dropout(0.3)(x)
num_classes = len(train_data.class_indices) 
output = Dense(num_classes, activation='softmax')(x)

# Train 6 Models

# MobileNetV3Large model
base_model = MobileNetV3Large(weights='imagenet', include_top=False, input_shape=(IMG_SIZE, IMG_SIZE, 3))

# MobileNetV2 model 
base_model = MobileNetV2(input_shape=(IMG_SIZE, IMG_SIZE, 3),
                         include_top=False,
                         weights='imagenet')

# EfficientNetB3 model
base_model = EfficientNetB3(
    weights="imagenet",
    include_top=False,
    input_shape=(IMG_SIZE, IMG_SIZE, 3)
)

# DenseNet121 model
base_model = DenseNet121(
    weights='imagenet',
    include_top=False,
    input_shape=(IMG_SIZE, IMG_SIZE, 3)
)

# EfficientNetV2S model
base_model = EfficientNetV2S(
    input_shape=(IMG_SIZE, IMG_SIZE, 3),
    include_top=False,
    weights='imagenet'
)
base_model.trainable = False

# ConvNext Model
base_model = Convnext(input_shape=(IMG_SIZE, IMG_SIZE, 3),
                         include_top=False,
                         weights='imagenet')

# Training 20 Epochs 
EPOCHS = 20

history = model.fit(
    train_data,
    validation_data=val_data,
    epochs=EPOCHS
)

# Accuracy Score for all models 
accuracy = accuracy_score(true_classes, predicted_classes)

print("Accuracy Score:", accuracy)
print("Accuracy Percentage:", accuracy * 100, "%")

# Calculate Precision, Recall, f1_score
from sklearn.metrics import precision_score, recall_score, f1_score

precision = precision_score(y_true, y_pred, average='weighted')
recall = recall_score(y_true, y_pred, average='weighted')
f1 = f1_score(y_true, y_pred, average='weighted')

print("Precision:", precision)
print("Recall:", recall)
print("F1 Score:", f1)

# Confusion Matrix
cm = confusion_matrix(y_true, y_pred)
plt.figure(figsize=(10,8))
sns.heatmap(cm, annot=True, fmt='d', xticklabels=class_labels, yticklabels=class_labels, cmap='Blues')
plt.xlabel('Predicted')
plt.ylabel('True')
plt.title('Confusion Matrix')

# Save the confusion matrix as an image
plt.savefig("/content/confusion_matrix.png")
plt.show()

print("Confusion matrix saved as /content/confusion_matrix.png")

# Save the TFLite model
tflite_model_path = "/content/mobilenetv3_large_cattle_breed.tflite"
tflite_model_path = "/content/mobilenetv2_cattle_breed_model.tflite"
tflite_model_path = "/content/EfficientNetB3_cattle_breed_model.tflite"
tflite_model_path = "/content/Efficient_cattle_breed_model.tflite"
tflite_model_path = "/content/DenseNet121_cattle_breed_model.tflite"
tflite_model_path = "/content/ConvNext_cattle_breed_model.tflite"
with open(tflite_model_path, "wb") as f:
    f.write(tflite_model)

print("TFLite model saved at:", tflite_model_path)




